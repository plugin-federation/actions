node_modules/
coverage/
*.log
.DS_Store
.idea/
.vscode/

# local reports from smoke runs
mcp-tools-list-report.json
*.sarif

# dist/ is COMMITTED (hermetic Action — consumers never npm install)
# rebuild with: cd mcp-tools-list-validate && npm ci && npm run build
